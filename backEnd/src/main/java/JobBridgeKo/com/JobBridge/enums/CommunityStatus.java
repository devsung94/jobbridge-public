package JobBridgeKo.com.JobBridge.enums;

public class CommunityStatus {

    public enum Category {
        FREE, QNA, REVIEW
    }

    public enum IsAnonymous {
        Y, N
    }
    public enum IsEditStatus {
        Y, N
    }
    public enum IsUse {
        Y, N
    }
    public enum Visibility {
        PUBLIC, PRIVATE
    }
}
